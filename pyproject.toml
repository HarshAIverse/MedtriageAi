[project]
name = "medtriage-ai"
version = "0.1.0"
description = "MedTriage AI OpenEnv environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
medical-triage-serve = "server.app:main"
